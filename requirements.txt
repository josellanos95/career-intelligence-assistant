# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
jinja2==3.1.5

# The LLM answers in Markdown; render it to sanitized HTML server-side for
# the chat bubble instead of showing raw "**"/"-"/"#" characters to the user.
markdown==3.7
bleach==6.2.0

# LLM providers
anthropic==0.42.0
openai==1.59.6

# Vector store (custom numpy-based brute-force cosine similarity, see README
# for rationale — no ANN index needed at this corpus size)
numpy==2.2.1

# Document parsing
# pymupdf preserves real line breaks and avoids kerning artifacts that pypdf
# introduces on design-heavy PDFs (see README: PDF parsing decisions)
pymupdf==1.25.1
python-docx==1.1.2

# Config & validation
pydantic==2.10.4
pydantic-settings==2.7.1

# Observability
structlog==24.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
